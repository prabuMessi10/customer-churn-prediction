# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Flask / runtime
instance/
*.log
.flaskenv

# Not needed for Streamlit hosting (kept only locally):
documentation/
dataset/
templates/
static/
app.py
run.bat
retrain.bat

# Training scripts (not needed at runtime).
# NOTE: src/preprocess.py and src/__init__.py ARE tracked — the saved model
# pipelines reference src.preprocess.clean_data and need it to unpickle.
src/train_models.py
src/tune_models.py
src/optimise_thresholds.py